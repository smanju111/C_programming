// factorial of n
// n!
// 1x2x3x4x5=5!
#include<stdio.h>
void main()
 {
 	int n, f, i;
 	scanf("%d", &n);
 	i=1;
 	f=1;
 	while(i<=n)   //f=fxi
 	{
 	 f=f*i;
	 i++;	
	 }
	 printf("\nFactorial = %d", f);
 }
 
