// find big number among x, y and z

#include<stdio.h>
     void main(){
	 int x, y, z;
     scanf("%d", &x);
     scanf("%d", &y);
     scanf("%d", &z);
     if(x>y)
     {
     	printf("x is big");
	 }
	 else if(y>z)
	 {
	 	printf("y is big");
	 }
	 else 
	 {
	 	printf("z is big");
	 }
}
